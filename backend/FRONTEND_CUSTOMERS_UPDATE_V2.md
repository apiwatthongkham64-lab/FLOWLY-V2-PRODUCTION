# Customers Update UI V2

API customer IDs are internal to each rendered row. The user never enters a database ID.

Flow: Customer row -> Edit -> Modal -> PUT /api/v1/customers/:id -> refresh API list
